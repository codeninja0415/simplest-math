import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Algebra",
  description: "This is Algebra Page",
  // other metadata
};

const AlgebraPage = () => {
  return (
    <div className="flex items-center justify-center grow w-full text-black dark:text-white">
      We are comming soon... ✌
    </div>
  );
};

export default AlgebraPage;
