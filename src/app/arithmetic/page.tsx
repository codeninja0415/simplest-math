import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Arithmetic",
  description: "This is Arithmetic Page",
  // other metadata
};

const ArithmeticPage = () => {
  return (
    <div className="flex items-center justify-center grow w-full text-black dark:text-white">
      We are comming soon... ✌
    </div>
  );
};

export default ArithmeticPage;
